from app.modules.{{ module_package }}.{{ entity_snake }}.services.store import _STORE


class {{ entity_pascal }}DeleteService:
  async def delete(self, resource_id: int) -> bool:
    for idx, row in enumerate(_STORE):
      if row.get("id") == resource_id:
        _STORE.pop(idx)
        return True
    return False
